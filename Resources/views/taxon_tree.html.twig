{% if not depth %}
    {% set depth = null %}
{% endif %}

{% if not template %}
    {% set template = 'knp_menu.html.twig' %}
{% endif %}

{% set menuItem = knp_menu_get('UnicatModuleBundle:TaxonMenu:tree', [], {
    'taxonClass': taxonClass,
    'css_class':  css_class,
    'depth':      depth,
    'routeName':  routeName,
    'taxonomy':  taxonomy,
}) %}

{{ knp_menu_render(menuItem, {
    'currentClass': 'active',
    'clear_matcher': false,
    'template': template,
}) }}
